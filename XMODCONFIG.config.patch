[
	{
		"op" : "test",
		"path" : "/ModsWithConfig"
	},
	{ 
		"op": "add", 
		"path": "/ModsWithConfig/OriModRedux", 
		"value": {
			"FriendlyName" : "Spirit Guardian Mod",
			"ConfigInfo" : [
				{
					"key" : "eventMessagesEnabled",
					"default" : true,
					"enforceType" : true,
					"display" : {
						"name" : "Enable event messages",
						"description" : "If enabled, your SAIL will display a message and give you a quest (with no requirements, so it's just a free reward) every March 11 to recognize the anniversary of Ori the Game."
					}
				},
				{
					"key" : "useSeinVoiceSounds",
					"default" : false,
					"enforceType" : true,
					"display" : {
						"name" : "Use Sein sounds",
						"description" : "If enabled, event messages will also play one of Sein's voice sound effects. This is disabled by default since they are quite unexpected and more of a surprise than a cohesion feature."
					}
				},
				{
					"key" : "debugAlwaysRunMessage",
					"default" : false,
					"enforceType" : true,
					"display" : {
						"name" : "^#FF1111;[Debug] ^reset;Always run events",
						"description" : "If enabled, the mod will behave as if the date is March 11 no matter what, causing the birthday message to display every time the player spawns."
					}
				}
			]
		}
	}
]